# packer-docker-vagrantfile

Vagrantfile with Packer and Docker 

## Usage

To get the latest release Packer and Docker versions:

```
make
```

If you'd like to compile the latest Packer code from source:

```
make source
```
